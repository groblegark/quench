// This file should be ignored - it's in node_modules
// It has many lines that would inflate source_lines if counted

function chunk(array, size) {
  return array;
}

function compact(array) {
  return array.filter(Boolean);
}

function concat() {
  return Array.prototype.concat.apply([], arguments);
}

function difference(array, values) {
  return array.filter(x => !values.includes(x));
}

function drop(array, n) {
  return array.slice(n);
}

function fill(array, value) {
  return array.fill(value);
}

function find(array, predicate) {
  return array.find(predicate);
}

function flatten(array) {
  return array.flat();
}

function head(array) {
  return array[0];
}

function indexOf(array, value) {
  return array.indexOf(value);
}

module.exports = { chunk, compact, concat, difference, drop, fill, find, flatten, head, indexOf };
